import React from "react";

const Dot = () => {
  //   <div>
  //     const [bgToggle, setBgToggle] = useState(false);
  // const [message, setMesaage] = useState(true);
  // const handleMessage = () => {
  //   const x =
  //     message && bgToggle
  //       ? console.log("hellow")
  //       : alert("plese fill the input");

  //   return x;
  // };
  return {
    /* <div className=" w-full mt-10 ">
      <div className="flex flex-col justify-center items-center">
        <div
          onClick={() => setBgToggle(!bgToggle)}
          className={`size-8 ${
            bgToggle ? "bg-blue-800" : "bg-[#F5F5F5]"
          } border  mb-2 cursor-pointer flex justify-center items-center`}
        >
          <div
            className={`bg-white size-4 rounded-xl ${
              bgToggle ? "block" : "hidden"
            }`}
          ></div>
        </div>
        <div>
          <div onClick={() => setMesaage(false)}>
            <button
              className="text-white bg-blue-700  border rounded-md px-12 py"
              onClick={handleMessage}
            >
              button
            </button>
          </div>
        </div>
      </div>
    </div> */
  };
};

export default Dot;
